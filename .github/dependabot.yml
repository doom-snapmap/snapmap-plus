# Propose updates to SHA-pinned GitHub Actions and their version comments.
# SDK and Python dependency pins are maintained separately.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    groups:
      actions:
        patterns:
          - "*"
